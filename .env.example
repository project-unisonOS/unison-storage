# Unison Storage Service
# Feature flags and security controls

# Require consent grants for protected endpoints. If storage exposes any
# protected data routes, turn this on to enforce consent scopes.
# Default: false (staged rollout)
UNISON_REQUIRE_CONSENT=false
